{
  "name": "webdev-starter-AntDesign",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "ant-design-vue": "^1.6.4",
    "nuxt": "^2.14.0"
  },
  "devDependencies": {
    "push-dir": "^0.4.1"
  }
}
